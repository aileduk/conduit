export const URL = "https://api.realworld.io/api/"

export enum Endpoints {
  Root = "/",
  Login = "login",
  Register = "register"
}

export enum UrlEndpoints {
  Articles = "/articles",
  Tags = "/tags",
  Profiles = "/profiles",
  Limit = "limit",
  Offset = "offset",
  Author = "author",
  Tag = "tag"
}
