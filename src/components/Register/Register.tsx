import Container from "../../layouts/Container"
import Form from "../Form/Form"

const Register = () => {
  return (
    <Container>
      <Form actionText='Sign up' accoutText='Have an account?' isRegister/>
    </Container>
  )
}

export default Register
